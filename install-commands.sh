# Install the Google API Client (Best for Gmail OAuth + Sending)
composer require google/apiclient

# Install Microsoft Graph (For Outlook integration)
composer require microsoft/microsoft-graph

# Install Laravel IMAP (For reading emails)
composer require webklex/laravel-imap

# Install Symfony Google Mailer (Alternative approach)
composer require symfony/google-mailer
 